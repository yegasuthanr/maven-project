pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean package'
            }
            post {
                sucess {
                    echo 'now archeiving....'
                    archieveArtifacts artifacts: '**/target/*.war'
                }
            }
        }
    }
}

