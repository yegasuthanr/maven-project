pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean PipelineAsPackage'
            }
            post {
                success {
                    echo 'now archeiving....'
                    archiveArtifacts artifacts: '**/target/*.war'
                }
            }
        }
    }
}

