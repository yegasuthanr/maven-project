pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean package'
            }
            post {
                success {
                    echo 'now archeiving....'
                    archieveArtifacts artifacts: '**/target/*.war'
                }
            }
        }
    }
}

