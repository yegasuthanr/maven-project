pipeline {
    agent any
    tools {
        maven 'localMaven'
    }
    stages {
        stage('Build') {
            steps {
                sh '''
                    echo "M2_HOME = ${MAVEN_HOME}"
                '''
            }
        }
    }
}

