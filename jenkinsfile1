pipeline {
    agent any
    tools {
        maven 'D:\Jenkins\apache-maven-3.5.3'
    }
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean package'
            }
            post {
                success {
                    echo 'now archeiving....'
                    archiveArtifacts artifacts: '**/target/*.war'
                }
            }
        }
    }
}

